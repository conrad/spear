import * as React from 'react';

let styles = require('./MenuBanner.scss');

export class MenuBanner extends React.Component {
  render() {
    return (
      <div className={styles.banner}>Spear</div>
    );
  }
}
